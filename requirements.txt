# Nexi core runtime
eel
pyttsx3
python-dotenv
SpeechRecognition
pyaudio
requests
pyautogui
pyperclip
beautifulsoup4
pillow
pywhatkit
openai>=0.28,<1
numpy
keyboard
pynput
psutil
comtypes
pywin32
hugchat
playsound

# Master intent router — local semantic tier (Tier 0).
# Optional: without it the router auto-falls back to a numpy-only lexical
# embedder (works, but the e5 model understands paraphrase/style far better).
sentence-transformers

# Testing
pytest

# Browser automation (scripts/playwright_runtime_check.py)
playwright

# Transitive deps for the above (kept for clarity)
typing_extensions
edge-tts

# --- Voice wake pipeline (required for run.py hotword/VAD/mic) ---
openwakeword
sounddevice
scipy

# --- Vision / camera control (hand, eye, face) ---
opencv-contrib-python
mediapipe

# --- Windows semantic desktop control (accessibility-first click/type) ---
uiautomation; sys_platform == "win32"

# --- Safe XML parsing ---
defusedxml
